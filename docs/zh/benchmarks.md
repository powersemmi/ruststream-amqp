# 基准测试 { #benchmarks }

在 `fe2o3-amqp` 客户端和你的处理器之间有两层，每一层在每条消息上都要花时间：这个 crate 把一条
链路变成一个订阅、把一条消息变成一次投递，框架的运行时再把这次投递分发给处理器。这一页把两者
分开测量，参照物是同样的活用这个客户端手写一遍。

同一个进程把每个场景跑三遍，三个循环各自只差一件事：由什么来承载消息。

- **裸客户端**直接驱动 `fe2o3-amqp`：接收、解码、读一个字段、接受投递。
- **适配器**驱动这个 crate 自己的消费者和发布者 —— Broker、地址描述符、订阅的流、投递的
  `ack` —— 由这里写的一个循环驱动，外面没有服务。
- **RustStream** 是用户写的整个服务：`#[subscriber]` 处理器、应用、运行时。

其余一切都保持相同：连接、每条链路所在的会话、链路的信用、投递保证、结算的位置、解码成同一个
类型、载荷字节、tokio 运行时和构建。这套流程属于框架本身，写在
[RustStream 基准测试页](https://powersemmi.github.io/ruststream/latest/zh/benchmarks/#methodology)
上；这一页公布它在这台机器上得出的结果。

## 数字 { #the-numbers }

交错轮次的中位数，括号里是观察到的离散范围。越大越好。

<div id="benchmark-results" data-benchmark-results="../../benchmarks/results.json" data-benchmark-labels='{"loading": "正在加载公布的结果...", "scenario": "场景", "raw": "裸客户端", "adapter": "适配器", "framework": "RustStream", "adapterOverhead": "适配器开销", "overhead": "总开销", "indistinguishable": "无法区分", "brokerBound": "受 Broker 限制", "machine": "机器", "os": "操作系统", "broker": "Broker", "build": "构建", "roundTrip": "往返", "versions": "版本", "measured": "测量于", "unavailable": "读不到结果。它们公布在 {url}。", "unknownSchema": "公布的结果声明的 schema 是 {schema}，这一页不渲染它。"}'></div>

表格由浏览器从上一次运行写下的文档读出，所以这一页上没有任何会过期的副本。

「适配器开销」是适配器对裸客户端：这个 crate 自己的消费者和发布者，在它们包装的客户端之上要花
多少，这个数字由本仓库负责。「总开销」是服务对裸客户端，中间多出运行时的那份工作。两者之差就是
运行时在这个 Broker 上所占的份额，之所以公布在这里，是因为它取决于两边如何衔接：订阅的流怎样
交出元素、投递怎样到达、背压怎样传回循环。

队列那一行只有一个消费者：一次投递到达、消息体解码、读一个字段、接受投递。请求-响应那一行在同
一次投递上再加发布的路径：应答方往请求指定的地址回答，并等 Broker 接受这个回复。两行里，发起
请求的一侧和生产者在三个循环中都是手写的，所以变化的只有接收的一侧。

标为「无法区分」的一行，是两半之间的差值小于各自多次运行之间离散范围的那一行。凡是协议本身比
它上面的代码贵得多的地方，这就是诚实的结果；低于运行噪声的数字读起来像是从未测到过的精度。

同一次运行的机器可读形式在
[`benchmarks/results.json`](https://powersemmi.github.io/ruststream-amqp/latest/benchmarks/results.json)，
框架的站点用它拼出跨 Broker 的汇总表。

## 机器 { #the-machine }

<div id="benchmark-environment"></div>

「往返」是传输自身的延迟，在所有循环之外测得：一次发送，客户端等它的 disposition，在一条连接上
取几万次。当一次投递所做的往返 —— 普通投递是零次，等待被接受的回复是一次 —— 占到这次投递开销的
一半以上时，这一行会被标为「受 Broker 限制」。这个标记意味着节奏由传输决定，旁边的数字是代码
开销的下界，而不是对它的测量。

构建标志和数字一起公布，因为它们会改变这些数字。用 `-C target-cpu=native` 构建出的二进制给出的
结果，换一台机器就复现不了，所以这条 recipe 在构建前先把这个变量清空。

## 这些数字不代表什么 { #what-they-do-not-mean }

这里只有一个消费者、一个地址、一个很小的消息体，以及一台跑在回环地址上的 Broker。它测的是一次
投递在这个 crate 里的开销，不是 Broker 能扛多少。这里的一行也不能拿去和另一个 Broker 公布的一行
比较：不同的传输在每条消息上做的事并不一样。换一个 AMQP 1.0 的 Broker，一行里的三个数字会一起
移动，所以镜像和它们一起公布。

一次运行的测量窗口从第一次投递开始，到最后一次投递的活干完为止，三个循环都是这样。结算在这个
时刻之后发生，所以一次运行携带的几十万个 disposition 里的这一个，在哪个循环里都落在数字之外。

负载在三个循环里、两个场景里都以预先结算的方式发出。如果生产者对每条消息都等一个 disposition，
这一行测的就成了这台 Broker 确认发送有多快，而被测的消费者会整个运行都闲着。

这些数字是一台机器在某一天的快照。它们按需重测，从不放进 CI：共享 runner 的噪声比这一页要讲的
差值还大。

## 自己跑一遍 { #running-it-yourself }

```bash
just bench
```

这条 recipe 从 `docker-compose.test.yml` 起停测试台，跑完两个场景，然后把测到的结果写回
`docs/benchmarks/results.json`。它要花半小时左右，并且需要整台机器。消息条数不是固定的：一次
试探运行会把它定下来，使得每一次被测量的运行在所在机器上都不短于五秒。
